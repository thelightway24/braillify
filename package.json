{
  "name": "braillify",
  "private": true,
  "version": "1.0.0",
  "description": "Braillify is a library for converting text to braille.",
  "type": "module",
  "scripts": {
    "lint": "pnpm -r lint",
    "test": "cargo tarpaulin --out xml --out stdout && vitest test --run && pnpm -r test",
    "build": "cargo build --release && pnpm -F braillify build && cd packages/python && maturin build --release --out dist"
  },
  "devDependencies": {
    "braillify": "workspace:*",
    "eslint-plugin-devup": "^2.0.5",
    "eslint": "^9.27.0",
    "vitest": "^3.1.3",
    "@vitest/coverage-v8": "^3.1.3",
    "@changesets/cli": "^2.29.4",
    "@types/node": "^22.15.19"
  },
  "author": "devfive",
  "packageManager": "pnpm@10.11.0",
  "resolutions": {
    "vite": "^6"
  }
}